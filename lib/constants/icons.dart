import 'package:flutter/material.dart';

abstract class BreedIcon {
  static const arrowNext = Icon(
    Icons.arrow_forward_ios,
    size: 10,
  );
  static const arrowBack = Icon(
    Icons.arrow_back_ios,
    size: 25,
  );
  static const nameSpace = SizedBox(
    width: 10,
  );

  static const favouriteOutline = Icon(Icons.favorite_outline);
}
